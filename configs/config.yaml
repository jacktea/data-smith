# sourceDb:
#   type: postgres
#   host: 192.168.11.220
#   port: 5432
#   user: example_user
#   password: obvious-placeholder
#   dbname: airedge2.1db
#   tableSchema: public

# targetDb:
#   type: postgres
#   host: 192.168.11.220
#   port: 5433
#   user: example_user
#   password: obvious-placeholder
#   dbname: airedge2.1db
#   tableSchema: public
# targetDb:
#   type: postgres
#   host: localhost
#   port: 5432
#   user: example_user
#   password: obvious-placeholder
#   dbname: airedge2.1db
#   tableSchema: public

sourceDb:
  type: postgres
  host: localhost
  port: 5432
  user: example_user
  password: obvious-placeholder
  dbname: airedge3.6db
  tableSchema: public

targetDb:
  type: postgres
  host: localhost
  port: 5432
  user: example_user
  password: obvious-placeholder
  dbname: airedge3.7db
  tableSchema: public

# 全局排除表: 迁移工具表等非业务表无需进行结构对比 (系统默认已包含 flyway_schema_history, schema_migrations)
# 如需额外排除其他表，可在此处配置追加
excludeTables:
  - flyway_schema_history
  - schema_migrations

